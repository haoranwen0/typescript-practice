import React from "react";
import { TimeLogProps } from "../type";

type Props = TimeLogProps;

const TimeLog: React.FC<Props> = ({ timeLog }) => {
  return (
    <div>
      <h1>Testing</h1>
    </div>
  );
};

export default TimeLog;
